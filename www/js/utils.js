// Generated by CoffeeScript 1.3.1
(function() {

  directory.utils.templateLoader = {
    templates: {},
    load: function(names, callback) {
      var deferreds,
        _this = this;
      deferreds = [];
      $.each(names, function(index, name) {
        return deferreds.push($.get('tpl/' + name + '.html', function(data) {
          return _this.templates[name] = data;
        }));
      });
      return $.when.apply(null, deferreds).done(callback);
    },
    get: function(name) {
      var template;
      template = this.templates[name];
      return template;
    }
  };

  window.showAlert = function(message, title) {
    if (navigator.notification) {
      return navigator.notification.alert(message, null, title, 'OK');
    } else {
      return alert(title + ": " + message);
    }
  };

}).call(this);
