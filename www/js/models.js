// Generated by CoffeeScript 1.3.1
(function() {
  var Contact, ContactCollection,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

  directory.models.Contact = Contact = (function(_super) {

    __extends(Contact, _super);

    Contact.name = 'Contact';

    function Contact() {
      return Contact.__super__.constructor.apply(this, arguments);
    }

    Contact.prototype.dao = directory.dao.EmployeeDAO;

    Contact.prototype.initialize = function() {
      _.bindAll(this);
      this.groups = new directory.models.ContactCollection();
      return this.groups.managerId = this.id;
    };

    Contact.prototype.getSocialData = function() {
      return console.log('getting social data');
    };

    return Contact;

  })(Backbone.Model);

  directory.models.ContactCollection = ContactCollection = (function(_super) {

    __extends(ContactCollection, _super);

    ContactCollection.name = 'ContactCollection';

    function ContactCollection() {
      return ContactCollection.__super__.constructor.apply(this, arguments);
    }

    ContactCollection.prototype.dao = directory.dao.EmployeeDao;

    ContactCollection.prototype.model = directory.models.Contact;

    ContactCollection.prototype.initialize = function() {
      return _.bindAll(this);
    };

    ContactCollection.prototype.findByName = function(key) {
      var employeeDao,
        _this = this;
      employeeDao = new directory.dao.EmployeeDAO(directory.db);
      return employeeDao.findByName(key, function(data) {
        return _this.reset(data);
      });
    };

    return ContactCollection;

  })(Backbone.Collection);

}).call(this);
